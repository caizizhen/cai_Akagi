manifest_version = 1

[bot]
name        = "example"
display     = "Example (rule-based)"
description = "Rule-based shanten optimizer. No knobs, ships in tree as a known-good reference."
version     = "0.1.0"

# No [settings.*] tables — this bot has nothing to configure. The frontend
# still receives a manifest, so it can show metadata even when the
# settings panel is empty.
[settings]
